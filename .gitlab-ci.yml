build_image:
  stage: build-image
  image: debian:latest
  script:
    - docker build -t debian:latest .

test:
  stage: test
  script:
    - echo "test"

build:
  stage: build
  script:
    - echo "build"

stages:
  - build_image
  - test
  - build