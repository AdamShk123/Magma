stages:
  - build_image
  - test
  - build

build_image:
  stage: build_image
  image: archlinux:latest
  script:
    - pacman -Syu
    - pacman -S docker
    - docker build -t archlinux:latest .

test:
  stage: test
  script:
    - echo "test"

build:
  stage: build
  script:
    - echo "build"