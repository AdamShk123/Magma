stages:
  - build_image
  - test
  - build

build_image:
  stage: build_image
  image: debian:latest
  script:
    - apt install -y docker
    - docker build -t debian:latest .

test:
  stage: test
  script:
    - echo "test"

build:
  stage: build
  script:
    - echo "build"